import { MenuTriggerDirective } from './menu-trigger.directive'

describe(MenuTriggerDirective.name, () => {
    it('should create an instance', () => {
        const directive = new MenuTriggerDirective()
        expect(directive).toBeTruthy()
    })
})
